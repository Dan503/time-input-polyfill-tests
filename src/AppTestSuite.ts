import { TimeInputTestSuite } from '..'

export const testSuite = new TimeInputTestSuite({
	localHostUrl: 'http://localhost:3000',
	primaryTestsLabel: 'Primary tests',
	eventTestsLabel: 'Event tests',
})
